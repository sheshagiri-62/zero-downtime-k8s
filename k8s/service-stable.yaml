apiVersion: v1
kind: Service
metadata:
  name: myapp-stable-svc
  namespace: zero-downtime
  labels:
    app: myapp
spec:
  selector:
    app: myapp
  ports:
    - name: http
      port: 80
      targetPort: 8000
      protocol: TCP
  type: ClusterIP
